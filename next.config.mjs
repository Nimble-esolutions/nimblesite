/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: false,
}; 
//export default {
 // output: 'export',
 // trailingSlash: true, // optional, helps with routing on shared hosting
//};
//export default {
  //output: 'export',
  //trailingSlash: true, // optional, helps routing on shared hosting
  // basePath: '/web-react-app',
//};

export default nextConfig;
